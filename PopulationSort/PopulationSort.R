# Challenge: Using the gapminder dataset (contained in gapminder_data.csv)
# Create a new data frame that contains the country names and the 2007 population, sorted by population in descending order.
# Print out the top 5 countries by population